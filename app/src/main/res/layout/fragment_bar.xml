<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.BarFragment">

    <LinearLayout
        style="@style/fragmentContentLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical">

            <ListView
                android:id="@+id/bar_listview"
                style="@style/listview"
                android:layout_width="match_parent" />

            <ProgressBar
                android:id="@+id/bar_listview_loading_progress_bar"
                style="@style/loadingProgressBar" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginLeft="25dp"
            android:layout_weight="3"
            android:gravity="center"
            android:orientation="vertical">

            <ListView
                android:id="@+id/bar_article_listview"
                style="@style/listview"
                android:layout_width="match_parent"
                android:dividerHeight="2dp"/>

            <ProgressBar
                android:id="@+id/bar_gridview_loading_progress_bar"
                style="@style/loadingProgressBar" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
